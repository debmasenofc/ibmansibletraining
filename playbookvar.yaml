---
- hosts: linux
  vars:
    test: welcome to training
    Test: I am Debmalya
  tasks:
  - name: print variable
    debug:
      msg="{{ test }}"
  - name: print another variable
    debug:
      msg="{{ Test }}"
